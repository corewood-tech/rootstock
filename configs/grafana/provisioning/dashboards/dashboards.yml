apiVersion: 1

providers:
  - name: Rootstock
    orgId: 1
    folder: Rootstock
    folderUid: rootstock
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards/rootstock
